// Deprecated placeholder: this project uses the flat config file `eslint.config.cjs`.
// Keeping this file (empty object) so tools or editors that look for .eslintrc.cjs
// won't break when expecting the file. Do not add legacy config keys like `env` here.

module.exports = {};
